# Backlog

Choses identifiées en cours de route, pas urgentes, à reprendre plus tard. Pas un todo de sprint —
juste une liste pour ne rien perdre. Voir aussi [CONTEXTE.md](CONTEXTE.md) (sections "En cours / pas
encore fait", "Zones grises à trancher" et "Debug / dette technique") pour le contexte détaillé
derrière certains de ces points.

| Sujet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Priorité |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| ~~Changement d'email sur la fiche utilisateur non répercuté sur le compte de connexion~~ (08/09/2026 → **corrigé le 08/09/2026**, voir CONTEXTE.md) : bug vécu en prod par Vincent — manager bloqué à la connexion après avoir changé son email depuis sa fiche, `modifierUtilisateurAdmin` ne mettait à jour que la table applicative, jamais `auth.users.email`. Corrigé par `synchroniserEmailAuth` (Server Action, `updateUserById` + `email_confirm: true`), appelée depuis `handleModifierIdentite` avant l'enregistrement de la fiche — bloque l'enregistrement plutôt que de laisser les deux diverger si la synchronisation échoue. | Moyenne |
| ~~Calendrier des employés : vue globale~~ (28/08/2026 → **fait le 28/08/2026**) : nouvelle vue heatmap (`CalendrierGlobal.tsx`), vue par défaut de `/suivre/calendrier`, même grille `MiniCalendrier` que la vue individuelle (identité graphique commune, cases en largeur pleine pour se toucher horizontalement — `PastilleJour.plein`) — dégradé orange clair → rouge foncé (échelle OrRd) piloté par MAX(ratio congés personnels pondérés en demi-journées, ratio CPI à 100%/DJI à 50%, communs à tous les actifs) / effectif actif ; les fériés restent en couleur dédiée `--color-ferie` plutôt que fondus dans le dégradé ; clic sur une date ouvrant une colonne de droite listant les absents (typologie) + bandeau dédié férié/CPI/DJI    ; états visuels dédiés (28/08/2026) — survol : chiffre en gras + léger grossissement, bulle "n collabs off"/"Jour férié" en fond slate (jamais sur un jour vide) ; sélection (clic) : fond blanc, bordure et texte dans la couleur du jour, état stable indépendant du survol réel (corrige la bulle qui disparaissait au clic, sensible au reflow provoqué par l'ouverture du panneau)    ; jours vides totalement non-interactifs (28/08/2026, demande explicite) — ni clic (pas d'ouverture du panneau), ni curseur pointeur, ni survol (`PastilleJour.plein.interactif`)    ; panneau de détail redessiné (28/08/2026, inspiré de DetailCongePanel — même largeur xl:w-64) : bandeau coloré = couleur heatmap du jour, titre date + "n collaborateurs off / nn demi-journées", corps groupé par section Journée entière/Matin/Après-midi, pill de période complète du congé (style HistoriqueTable, classeBordureTypeBadge)    ; en-tête avec JourBadge (abréviation 2 lettres, ex. "Ma") devant les 2 lignes titre+stats plutôt que le nom du jour en toutes lettres, carte à coins carrés    ; corps du panneau finalisé (28/08/2026) : avatar collaborateur retiré, typos ajustées (section text-sm, nom text-xs text-ink-500), contenu indenté sous le titre de section, pill de congé alignée sur le format `SoldeDetailPanel` (point de statut validé/en attente + "CODE : dates", ex. "CP : 21/09 au 25/09/26"), sous-titre stats abrégé "N Collab. off · N demi-jour."    ; finitions (28/08/2026) : titre de page "Calendrier consolidé" (sous-titre "Calendrier des employés" retiré), message dédié "Personne ne travaille aujourd'hui !" (centré) sur un jour férié, DJI intégrée comme ligne "fluo" (bg-dji/15 text-dji) dans la section Matin/Après-midi correspondante plutôt qu'un bandeau séparé (bandeau restant réservé au CPI)    ; grille des mois recentrée dans le corps de page, espacement resserré à 15px entre les cards (mx-auto justify-center gap-[15px], largeurs des cards recalculées en conséquence)    ; largeur max des cards mois plafonnée à 256px (valeur du palier 3 colonnes, la plus haute résolution — évite l'étirement à ~391px au palier 2 colonnes)    ; distribution des mois corrigée (28/08/2026, demande explicite) — colonnes toujours calées à gauche (retrait du centrage par ligne, qui recentrait la dernière ligne incomplète type "5 mois affichés"), espacement resserré à 10px entre cards et entre la grille et le panneau de détail (colonne grid passée de `minmax(0,900px)` à `max-content` — sinon la colonne réservait 900px indépendamment du contenu réel, laissant un vide avant le panneau malgré le gap réduit) | Urgente |
| ~~Bug transition `scale`/`transform` dans `SoldeCard.tsx`~~ (20/08/2026 → **corrigé, 02/09/2026**) : découvert en corrigeant le même bug dans `SoldeDetailPanel.tsx` — voir [SUIVI-SOLDE.md](SUIVI-SOLDE.md) — `transition-[background-color,box-shadow,transform]` ciblait `transform` pour animer `hover:scale-105`, alors que Tailwind v4 anime le survol `scale-*` via la propriété CSS native `scale` (le grossissement au survol se faisait donc instantanément au lieu d'être animé en douceur sur 200ms) ; `transform` remplacé par `scale` ligne 61. | Faible |
| ~~Gérer les effets over sur les composants du calendrier~~ (→ **fait, confirmé par Vincent le 28/08/2026**) | Moyenne |
| ~~Améliorer l'interface/UI de la page Paramétrer > Congés & RTT~~ (→ **fait, confirmé par Vincent le 28/08/2026** ; **passe complémentaire le 05/09/2026** : cards coins carrés, `SelectPille`, labels navy gras et contour vert bouton — conventions de formulaire stabilisées sur la fiche utilisateur/popin création réappliquées ici, voir CONTEXTE.md) | Faible |
| ~~Préciser les règles de paramétrage d'un nouveau calendrier — décembre~~ (→ **livré le 10/09/2026**, voir CONTEXTE.md) : liste d'années dynamique sur Paramétrer > Calendrier (archivées 4 dernières années en lecture seule + année en cours civile par défaut + brouillon(s), publier révèle automatiquement le brouillon suivant). Fix associé trouvé en testant : faux "Conflit d'agenda" à la création d'un CPI (comptait à tort la demande CP auto-générée par le CPI comme un conflit avec lui-même). **Reste** : notif Delphine (au-delà du bandeau visuel de décembre déjà existant) pas implémentée ; écrans collaborateur (Dashboard/Suivre calendrier) toujours limités à une fenêtre fixe de 3 années, pas étendus si publication avance de 2+ ans sur l'année civile (cas non rencontré en pratique) | Haute |
| ~~Refonte du système de définition des CPI/DJI/Fériés côté admin~~ (18/08/2026 → **faite le 22/08/2026**, voir CONTEXTE.md) : les 3 anciennes modales remplacées par des tiroirs sous les cartes légende + une popin unifiée `ModalPoserJourImpose` ; alerte de transparence sur chevauchement avec une demande perso implémentée sous le nom "Conflit d'agenda" (tiroir dédié, une ligne par couple entrée/demande) | Faible |
| ~~Jours fériés : vérifier le fonctionnement... CPI/DJI/Fériés pas encore définis par l'admin~~ (→ **vérifié le 28/08/2026**) : le collaborateur peut poser un congé normalement — Delphine reçoit une alerte "Conflit d'agenda" listant les congés déjà pris par des collaborateurs à ces dates | Moyenne |
| ~~Règle de gestion des soldes et feeds soldes~~ (18/08/2026, → **tranché le 28/08/2026**) : le modèle final ne garde que 2 notions — **Réel** (passé en paie) et **Théorique** (incluant les demandes validées ou en attente), plus de 3e valeur "Solde en cours" séparée | Haute |
| ~~Nettoyage cosmétique de nommage : `Dashboard2Page`~~ (→ **fait le 28/08/2026**) : fichier renommé `components/dashboard/DashboardPage.tsx`, composant `DashboardPage` — un seul vrai import à jour (`app/(app)/page.tsx`), tous les commentaires JSDoc dans les 12 autres fichiers mis à jour | Faible |
| ~~Harmonisation UI restante (audit du 14/08/2026)~~ (→ **remplacé le 28/08/2026** par l'item consolidé "Uniformisation des éléments UI" ci-dessous) | Moyenne |
| ~~"Passé en paie" : un statut sur une demande pour savoir si un congé a déjà été envoyé à la comptable~~ (→ **couvert autrement, 28/08/2026**) : pas de nouveau statut créé, mais le besoin est satisfait par la présence de `export_paie_lignes`, déjà utilisée partout (badge "Transmis" dans `HistoriqueTable`, `peutAnnulerValide`/`dejaTransmis` dans `DetailCongePanel`, colonne "Paie") — résolu en pratique, juste pas via l'approche envisagée à l'origine | Moyenne |
| ~~Revoir l'ensemble des règles de gestion d'Export paie et du suivi des congés~~ (→ **fait, confirmé par Vincent le 28/08/2026**) | Moyenne |
| ~~Clic sur une ligne d'Historique (`/historique`)~~ (→ **fait le 28/08/2026**) : `HistoriqueTable.tsx` rend maintenant la ligne entière cliquable (`<tr onClick>`), plus seulement la pastille de dates — audit fait sur toute l'app, 2 exceptions volontaires laissées telles quelles (2 actions distinctes par ligne) : `SuivreSoldesPage2.tsx` (Théorique/Réel) et `CongesRttPage.tsx` (Modifier/Supprimer) | Moyenne |
| ~~Calendrier simplifié — reste à trancher~~ (15/09/2026, demande de Vincent, plusieurs points sur le calendrier de la home `DashboardPage.tsx` → **tous traités le 15/09/2026**) : ~~(1) quel nombre de mois affichés — 9 ou 12~~ (confirmé : on reste sur 9) ; ~~(2) matérialiser les jours passés~~ (chiffre atténué opacité 50%, sauf jour déjà posé — `estPasse` sur `MiniCalendrier`) ; ~~(3) navigation~~ (sélecteur "Commence : Aujourd'hui / Il y a 3 mois", décale le début de la fenêtre de 9 mois sans changer sa largeur) ; ~~(4) bug affichage d'une période associée à une demi-journée~~ (chevauchement demande/férié/DJI, `tipoDuJour`/`occupantDuJour`) ; ~~(5) propager vers `/suivre/calendrier`~~ (`CalendrierCollaborateur.tsx` entièrement refondu sur le même gabarit — fenêtre 9 mois, 4ᵉ colonne `DetailCongePanel`/`DetailJourCommunPanel`, couleur du chiffre, chevauchement, `moitieCliquee`, `estPasse` ; nuance "déjà transmis en paie" branchée le 15/09/2026 (`lignesTransmissionParDemande`) ; **droits revus le 15/09/2026** (demande explicite de Vincent — "les droits des managers sont les mêmes que ceux des administrateurs avec juste un droit supplémentaire : la validation des congés") : manager ET admin peuvent tous les deux Annuler à tout moment, y compris déjà transmis en paie (`peutAnnulerDejaTransmis` toujours `true`), manager garde en plus Valider/Refuser ; Régulariser abandonné (même changement appliqué à `SuivreDemandesPage.tsx`, qui avait le même reliquat)) | Moyenne |
| ~~Bug dépôt de congé : gestion de la demi-journée quand la période finit sur une DJI matin~~ (20/08/2026, probablement déjà traité → **confirmé par Vincent le 15/09/2026**) : le verrouillage du sélecteur de demi-journée sur une DJI (18/08/2026, `djiSurDate`/`demiParDefautDebut`/`demiParDefautFin`, `PoserDemandeModal.tsx`) couvre bien ce cas de fin de période | Moyenne |
| **Tester et modifier la couleur des congés CP** (15/09/2026, demande de Vincent → **longue session de tests le 15/09/2026, aucune teinte retenue, voir CONTEXTE.md pour le détail**) : le modèle a changé en cours de route — la distinction validé/en attente n'est plus la couleur du chiffre (chiffre toujours blanc désormais) mais un contour orange `ring-status-warning-fg` autour de la pastille (voir item "Calendrier simplifié" plus haut). Calcul de contraste WCAG fait sur la palette d'origine : aucune teinte (CP/RTT/CPA/CSS/CE/RECUP/EVT_FAM/CPI/DJI/FE) ne passe le seuil texte blanc (4.5) ni le seuil contour non-textuel (3), y compris les couleurs déjà en prod avant cette session. Plusieurs pistes essayées et toutes rejetées par Vincent : turquoise/vert profond isolés, palette "jewel tones" sombre complète (trop terne, "moche et illisible"), interversion CP/CPI, alignement de plusieurs types sur une même teinte — revenu à la palette d'origine à chaque fois. Reste ouvert : trouver une combinaison lisible ET élégante ET différenciable reste à faire, la contrainte "chiffre blanc partout" rend l'exercice difficile avec des teintes pastel/moyennes. **Piste à tester la prochaine fois** (15/09/2026, fin de session) : abandonner le "chiffre blanc partout" — couleurs de fond plus pâles, chiffre en encre foncée ET contour "en attente" en foncé (plutôt que blanc/orange sur fond saturé) ; peut-être aussi revoir la forme "pastille ronde" du calendrier elle-même (`MiniCalendrier.tsx`), pas seulement les couleurs. **Piste testée le 16/09/2026, à nouveau abandonnée** : fond pâle (15% sur blanc, `color-mix`) + chiffre en encre foncée dédiée par type (CP `#04344C`, RTT `#2A5D35`, généralisé ensuite à `#04344C` partout) + contour "en attente" assombri quand nécessaire (`#b56b00` pour CP) — étendu à DJI (nouvelle couleur turquoise `#23CED9` + texte `#054952`) et à CE/CSS/RECUP/EVT_FAM (couleur rose partagée `#F48B9A`), avec un aller-retour d'interversion CPA ↔ CE/CSS/RECUP/EVT_FAM en cours de route. Rendu vérifié en direct et fonctionnel (calendrier, headers `DetailCongePanel`/`DetailJourCommunPanel`, badges `TypeBadge`, compteur solde CPA de l'accueil) mais **finalement abandonné dans son ensemble** : décision de revenir aux couleurs d'origine de la palette (aucun swap, aucun fond pâle) et de ne garder qu'un seul changement — le chiffre des jours posés repasse en **blanc** (retour sur l'essai "encre foncée" du 15/09/2026 lui-même). Aucun commit fait entre-temps, retour arrière propre par `git checkout`. Corollaire trouvé en testant : la fusion visuelle CPI/DJI sous une seule couleur (`var(--color-cpi)`, décision du 20/08/2026) fait qu'une DJI "pure" (sans période CPI réellement configurée) affiche à tort la couleur CPI — non re-corrigé après l'abandon de la session, `communDuJour()` dans `DashboardPage.tsx`/`CalendrierCollaborateur.tsx` reste sur `var(--color-cpi)` | Faible |
| ~~Section "what's up" pour le dashboard manager et administrateur~~ (15/09/2026, demande de Vincent → **les 4 volets couverts ou relocalisés le 21/09/2026**) : nouvelle section d'accueil orientée manager/admin, à l'image du bloc "Depuis ma dernière visite" côté collaborateur — 4 volets identifiés : ~~tâches en cours (manager, ex. demandes en attente de sa décision)~~ — **`DemandesAEtudierCard` ouverte à l'admin le 18/09/2026** (visible manager uniquement jusqu'ici) ; ~~congés récemment validés (admin)~~ — **recadré le 21/09/2026**, voir l'item "Suivre les demandes : vue Kanban" ci-dessous (remplace l'idée de card/compteur par un kanban permanent) ; ~~qui est in/qui est out (qui est en congé aujourd'hui)~~ — **`CollaborateursEnCongeCard` livrée le 18/09/2026** (voir CONTEXTE.md) : encart coloré avec le même dégradé que le heatmap de "Calendrier consolidé" (`lib/heatmap.ts`), version simplifiée sans fériés/CPI/DJI communs, à affiner ; ~~tâches à venir pour Delphine (paramétrage calendrier, export paie, etc.)~~ — **regroupé le 21/09/2026** avec l'item "Suivre les demandes : vue Kanban" ci-dessous | Haute |
| **"Suivre les demandes" : vue Kanban** (21/09/2026, cadrage et itérations en direct avec Vincent — voir CONTEXTE.md pour la discussion complète et l'état final) : `/suivre/demandes` gagne 2 vues (toggle Kanban/Liste, Kanban en premier — la Liste actuelle, `HistoriqueTable`, reste inchangée). **Scope final, 4 zones** : **(1) En attente de validation**. **(2) Prochain export** — une seule colonne avec 3 sous-groupes optionnels (sous-titre affiché seulement s'il y a au moins une card) : "Congés annulés" (correction pas encore transmise), "Périodes précédentes" (backlog jamais transmis), "Congés du mois" (cas normal, sous-titre affiché seulement si un des 2 autres sous-groupes est aussi présent). **(3) Exports futurs** — congés validés pas encore dus, avec un sélecteur de mois intégré au titre de la colonne (texte souligné + chevron, pas de select séparé). **(4) Refusées et annulées** — nouvelle colonne archive, repliée par défaut (bande verticale cliquable), fond rouge. Clic sur une card ouvre `DetailCongePanel` (réutilisé tel quel, `pleineLargeur` + hauteur alignée sur la ligne kanban) — l'interaction "le panneau pousse les colonnes voisines" (mockups Vincent d'origine) reste à designer, pas encore implémentée. Filtres "Statut"/"Période" de la page masqués en vue Kanban (n'ont plus de sens, le statut est la classification elle-même). Même accès manager/admin que l'écran actuel (`fetchDemandesEquipe`, RLS inchangée). **Explicitement écarté après réflexion** : des colonnes Transmise/En paie/Refusée séparées (1ère itération à 5 colonnes) — feraient doublon avec "Quels congés transmettre"/"Vérifier les fiches de paie", qui remplissent déjà bien ce rôle. **Reste à faire** : pagination au-delà de 15 cards/sous-groupe, interaction du panneau glissant. | Moyenne |
| **"Tâches à venir pour Delphine"** (15/09/2026, ex-4ᵉ volet de l'item "what's up" → **dissocié du Kanban et mis de côté le 21/09/2026**, Vincent) : paramétrage calendrier, export paie, etc. — pas du tout cadré, aucune fonction repository dédiée aujourd'hui. Repris séparément à une prochaine session, pas de dépendance avec le Kanban "Suivre les demandes" ci-dessus. | Faible |
| **Audit resize/breakpoints & mobile collaborateur** (fusionné le 02/09/2026 — ex-"Adaptation de l'expérience collaborateur sur mobile" + "Audit global des principes de resize/breakpoints") : passer en revue les écrans côté salarié sur petit écran (pas encore audités dans leur ensemble), ET vérifier systématiquement le comportement de toutes les pages à chaque breakpoint Tailwind (pas juste au pif) — au moins deux cas connus de la même famille de bug déjà rencontrés (contenu fixe + contenu flexible qui ne tiennent pas ensemble au moment précis où le layout bascule en côte-à-côte) : `CalendrierPage.tsx` à 768px (grille 900px + légende 256px) et le panneau détail d'Export paie sous 1280px (déjà corrigé, seuil remonté de `md:` à `xl:`) — probablement pas les deux seuls cas dans l'app | Moyenne |
| **Refonte UI / visibilité des messages de confirmation et des toasters** (fusionné le 02/09/2026 avec "Checker le format/l'UI du contenu des popins de confirmation") : revoir la visibilité des toasters, ET passer en revue le texte/la mise en forme du contenu des différentes popins de confirmation forte (`demanderConfirmation`/`Modal`) déployées dans l'app — DetailCongePanel (Refuser/Annuler/Transmettre) et « Ajuster le solde » (SoldeDetailPanel, ajouté le 02/09/2026) — cohérence de formulation et de style entre elles | Moyenne |
| **Solde négatif RTT/CPA sur l'Accueil** (10/09/2026, remarque de Vincent après avoir déplafonné la fenêtre de consommation CPA cette même session — une demande anticipée très en avance peut désormais faire passer le solde théorique CPA en négatif, sans garde-fou ni indicateur dédié → **simplifié le 21/09/2026 en question métier à adresser à Abeil**) : autorise-t-on l'affichage d'un solde négatif pour RTT et CPA, ou bascule-t-on sur le solde réel sur la page d'accueil dans ce cas ? | Moyenne |
| Gestion du "Journal" côté manager : permettre à l'utilisateur (manager) de visualiser ses propres validations/refus de demandes — pendant du "Journal" déjà existant côté collaborateur (Accueil, `ActiviteRecenteFeed`), à décliner pour l'historique des décisions prises | Moyenne |
| **Rédiger le contenu des mentions légales** (10/09/2026, page `/mentions-legales` créée en placeholder avec le pied de page, voir item "Ajouter un pied de page" plus bas) : identité de l'éditeur (raison sociale Citizen D, forme juridique, adresse siège, RCS/SIREN, capital social, TVA intracommunautaire, contact), directeur de la publication, hébergeur (Vercel Inc.), et politique de confidentialité RGPD séparée (l'outil traite des données RH réelles — noms, dates de congés, soldes). Bloqué sur les infos juridiques réelles de Citizen D que Vincent doit fournir. | Moyenne |
| Lisibilité typo et accessibilité : audit global — tailles/contrastes de texte parfois très réduits (ex. `text-[10px]`/`text-[11px]` sur plusieurs feeds et popins), pastilles de couleur seules sans alternative texte pour les daltoniens, navigation clavier et lecteurs d'écran non vérifiés sur les popins/tiroirs/modales | Moyenne |
| ~~Authentification : définition de mot de passe (première connexion) et parcours "mot de passe oublié"~~ (02/09/2026, précisé et priorité relevée → **fait et validé en prod le 08/09/2026**) : SMTP custom Resend (domaine `abeil-conges.citizen-d.fr` sur `citizen-d.fr`, propriété de Vincent) ; invitation via `supabase.auth.admin.inviteUserByEmail` (`lib/supabase/admin.ts`) déclenchée à la création d'un utilisateur ; écran partagé de définition de mot de passe (double saisie + check, icônes œil, règle 8 caractères/majuscule/caractère spécial factorisée dans `lib/passwordPolicy.ts`) pour l'invitation ET "mot de passe oublié" ; boutons "Renvoyer l'invitation"/"Envoyer un lien de réinitialisation" sur la fiche utilisateur ; toaster de confirmation ; templates email en français. Origine du site déduite des en-têtes de requête (`lib/siteUrl.ts`), fonctionne en local et en prod sans variable à changer. Plusieurs bugs Supabase/Next.js trouvés et corrigés en testant (détail complet dans CONTEXTE.md) : troncature des paramètres de requête dans les liens email (contourné en passant `type` par segment de route), templates utilisant `{{ .SiteURL }}` (figé sur localhost) au lieu de `{{ .RedirectTo }}`, préchargement `<Link>` grillant le token à usage unique en prod, `SUPABASE_SERVICE_ROLE_KEY` absente des env Vercel, message d'erreur trompeur pour un mot de passe identique à l'ancien. **Reste** : évaluer une version HTML/stylée des emails (actuellement texte brut) — voir item dédié ci-dessous. | Haute |
| ~~Notifications email des nouvelles demandes de congés pour admin/manager~~ (02/09/2026 → **volet réglages/immédiat/digest livré et validé en prod le 08/09/2026**, voir CONTEXTE.md) : nouvelle page `Paramétrer > Notifications` — radio immédiat/récap hebdomadaire (jour + heure de bureau, dégradée en cron quotidien 18h UTC sur le plan Vercel Hobby), copie carbone administrateur optionnelle, lien de l'email vers les demandes en attente. **Volet "déléguer les droits de validation de congés" explicitement mis de côté pour un chantier séparé** (touche la RLS de `demandes_conges`/`decisions_demande`, `proxy.ts`, `SuivreDemandesPage.tsx` — table `delegations_validation` déjà existante mais inutilisée). | Haute |
| ~~Session dédiée sur les grilles/mises en page d'Accueil~~ (Vincent, 18/08/2026) — **audit fait le 18/08/2026** : la grille Soldes (colonnes `1fr`, grandit pour remplir l'espace) et la grille calendrier "Mes Congés" (`auto-fit minmax(170px,1fr)`, pilotée par son contenu) restent deux logiques différentes, mais **l'objectif d'alignement pixel-parfait entre les deux a été explicitement abandonné** ("On abandonne cet objectif") — le hack de "colonne fantôme" qui tentait de les faire correspondre a été retiré. Largeur globale du projet harmonisée le même jour : un seul `max-w-[1180px]` centré, porté par `AppShell`/`HeaderBar`, les caps `md:max-w-6xl` dupliqués dans 8 pages retirés | Faible |
| ~~Filtres "Validés non vus"/"Refusés non vus" sur Historique~~ (→ **retirés le 28/08/2026**) : aucun lien ne pointait vers ces filtres (le compteur Accueil censé les déclencher n'existe pas), et ce n'est pas un critère qu'un utilisateur choisit manuellement — retirés du menu `Filtre`/`HistoriquePage.tsx` ; le mécanisme `vu`/`marquerVue` reste utilisé par le Journal (Accueil) | Faible |
| ~~Limites connues du "vu" par session~~ (→ **simplifié le 10/09/2026**, voir CONTEXTE.md) : le principe "vu depuis votre dernière connexion" (`sessionStorage`/`localStorage`, ni une vraie session d'authentification ni scopé par utilisateur) est remplacé par un principe plus simple sur demande de Vincent — une décision reste surlignée tout le temps où le tiroir "Mon journal" est ouvert, et n'est marquée vue qu'à sa **fermeture** (`fermerTiroirActivite`, `DashboardPage.tsx` — un premier essai au moment de l'ouverture faisait disparaître les nouveautés avant d'avoir pu les lire). Toujours pas d'équivalent côté manager (voir "Gestion du Journal côté manager" plus haut) | Faible |
| ~~Statuer sur les composants dashboard orphelins restants~~ (→ **supprimés le 28/08/2026**) : `ActiviteRecenteTable.tsx`, `ActiviteRecenteListe.tsx`, `ActiviteRecenteCard.tsx` et la route `/preview-orphelins` retirés (aucun vrai import ailleurs, juste des mentions en commentaires) — `ProchainsJoursOffCard.tsx` conservé, activement utilisé sur Accueil | Faible |
| ~~Test de la chronologie et de l'évolution des soldes... : mettre en place un environnement permettant de tester les dates et les événements~~ (priorité relevée le 28/08/2026 → **dispositif livré le 10/09/2026**, voir CONTEXTE.md) : bandeau de date simulée en local (`DateOverrideBanner.tsx`/`lib/aujourdhui.ts`, jamais en prod), branché sur le moteur de soldes et le Calendrier (bandeau décembre, bascule d'année) — reste à étendre aux autres `new Date()` de l'app si besoin (filtres "année en cours" cosmétiques) | Moyenne |
| **Prise en compte des dates de vacances scolaires** (20/08/2026, précisé le 28/08/2026 — regroupe aussi le volet "zones de congés scolaires" de l'ex-"Paramétrage des congés", fusionné le 02/09/2026) : admin sélectionne une/des zone(s) de vacances scolaires (A/B/C) en back-office, surlignage correspondant sur les calendriers — **aucune interaction avec les demandes de congés** (pas de blocage, pas de conflit, pas d'impact solde), purement visuel. Architecture pressentie : même pattern que fériés/CPI/DJI (table de référence de périodes + overlay fusionné sur les calendriers, `DetailPeriodeConges`/resolveur d'occupant) — probablement plus simple que ces derniers puisque sans les règles métier associées. Point à trancher avant de coder : zone **globale** entreprise ou **par collaborateur** (télétravailleurs répartis sur plusieurs zones) ? Les zones A/B/C sont des zones régionales officielles définies par le ministère de l'Éducation nationale, chaque zone ayant ses propres dates de vacances scolaires — donc une simple table de référence "zone + période + dates" à saisir/maintenir chaque année, sans logique de calcul propre (contrairement aux règles d'acquisition CP/RTT). | Faible |
| **Passage en multi-tenant pour dupliquer le projet à d'autres clients** (20/08/2026, chiffré et détaillé le 08/09/2026 → **fondations schéma + RLS, sécurisation des points RLS-bypass, branding par entreprise (couleurs + logo), résolution du tenant par sous-domaine/chemin, flux d'onboarding d'un tenant, et e-mails d'invitation brandés par tenant livrés le 09/09/2026**, voir CONTEXTE.md et [MULTI-TENANT.md](MULTI-TENANT.md) pour la référence détaillée) : table `entreprises` + colonne `entreprise_id` sur 19 tables (défaut vers Abeil, migration non-cassante), helper `my_entreprise_id()`, 64 policies RLS scopées, `objectifs_calendrier`/`parametrage_notifications` passées en une ligne par entreprise. `types_absences`/`jours_feries` restent des tables de référence globales (décision actée avant de coder). Les fonctions notifications qui tournent en `service_role` filtrent maintenant explicitement par `entreprise_id`. Couleurs de charte (`--color-brand-primary`/`--color-brand-accent`, ex-`abeil-navy`/`abeil-yellow`) configurables par entreprise pour l'app connectée, appliquées via une variable CSS injectée au niveau du layout (`app/(app)/layout.tsx`/`AppShell.tsx`) — scope volontairement réduit à ces 2 tokens, pas `--color-slate` (couleur "de travail" encore utilisée pour la plupart des boutons/liens, chantier séparé "Refacto & récap Design System"). **Recentré le 09/09/2026** (test visuel sur un header orange a montré que la couleur se propageait à tort aux H1/labels/montants de solde) : la couleur de tenant ne s'applique plus qu'au header (`HeaderBar`) et à la nav secondaire (`SideNav`/`BottomNav`) — tout le contenu de page redevient générique (`text-ink-900`), 80 usages corrigés dans 20 fichiers. Colonne `entreprises.slug` + route publique `service_role` `app/api/branding-public/route.ts` (résolution par en-tête `Host`, jamais une policy RLS ouverte) : la page de connexion elle-même affiche désormais le bon branding par sous-domaine, avant authentification — sans propagation du tenant dans les Server Actions (pas nécessaire, la RLS post-connexion s'en charge déjà indépendamment du sous-domaine d'arrivée). Logo par entreprise (`entreprises.logo_url`/`logo_url_fond_clair`/`logo_url_signe`, text nullable, URL saisie à la main — pas de Supabase Storage, non justifié pour un seul tenant réel) : header, page de connexion et signe SideNav affichent désormais le logo du tenant, `null` ⇒ fallback sur les fichiers Abeil en dur. **Flux d'onboarding** (`/admin`, nouveau statut `super_admins` orthogonal à `role`/`entreprise_id` — aucun rôle existant n'a d'autorité au-dessus d'une seule entreprise, premier super-admin `vincent.mayol@gmail.com`) : formulaire de création d'un tenant (nom, slug, couleurs optionnelles, premier admin invité par email), `assertSuperAdmin()` revérifié à chaque point `service_role` (page ET Server Action), rollback best-effort si la création échoue en cours de route. Testé à chaque étape : non-régression + tests réels avec des entreprises de test (isolation des données, couleurs/logo qui changent bien, gate `/admin` confirmée pour un compte non super-admin, création de bout en bout avec le compte réel de Vincent). **E-mails d'invitation brandés par tenant** (`lib/resend/invitation.ts`) : remplace l'envoi natif Supabase (template unique pour tout le projet) par un envoi maison via Resend — `generateLink` crée le compte sans envoyer d'e-mail, l'app construit et envoie le sien (logo/nom du tenant, domaine dédié `notifications@apidays.citizen-d.fr` distinct de celui d'Abeil). Les pages `/connexion/confirmer/*`/`définir-mot-de-passe` sur lesquelles ce lien atterrit sont maintenant brandées aussi (`slug` propagé sans risque via le lien qu'on construit nous-mêmes). **Isolation vérifiée** (09/09/2026, à la demande de Vincent) : entreprise de test avec données réelles (demande, ajustement, FAQ, admin + salarié), RLS testée directement (session authentifiée tentant de lire les données de l'autre tenant par id connu, dans les deux sens) ET via le parcours applicatif — aucune fuite constatée. Connexion depuis l'espace d'un tenant avec les identifiants d'un autre tenant désormais explicitement rejetée (`app/connexion/actions.ts`, message dédié) — trouvé et corrigé pendant cet audit. **Fuite de données cross-tenant trouvée et corrigée** (09/09/2026) : `demi_journees_imposees` (renommée depuis `rtt_imposes` avant le chantier multi-tenant) avait conservé 2 policies RLS fantômes de l'ancien nom (sans filtre `entreprise_id`, jamais mises à jour par la passe de réécriture RLS car attachées à l'ancien nom) — combinées en OR avec les bonnes policies, elles ouvraient la table à tout utilisateur authentifié, tous tenants confondus. Trouvé par Vincent sur test3 (lisait les 32 lignes d'Abeil), corrigé (`drop policy` des 2 fantômes), revérifié : isolation rétablie, accès légitime d'Abeil intact. **Audit étendu à toutes les tables** (comparaison du nombre de policies par table entre schema.sql et l'état réel) a trouvé une seconde table en écart, `export_paie_lignes` (doublon d'INSERT sans filtre `entreprise_id`, nom tronqué à 63 caractères — faille d'écriture cross-tenant, corrigée pareillement). Les 23 autres tables sont conformes. Voir MULTI-TENANT.md pour le détail et la leçon (vérifier le nombre de policies par table après tout renommage). **5 bugs critiques trouvés et corrigés en exerçant un vrai 2ᵉ tenant ("test3") via la vraie UI** (09/09/2026) : (1) le `default` d'`entreprise_id` sur 18 tables pointait en dur vers l'uuid Abeil, cassant silencieusement l'écriture pour tout autre tenant — basculé vers la fonction `my_entreprise_id()` ; (2) les tables singleton (`objectifs_calendrier`/`parametrage_notifications`) n'étaient jamais seedées à la création d'un tenant — `creerTenant()` s'en charge désormais ; (3) les `UPDATE` de ces 2 mêmes tables avaient perdu tout filtre `.eq(...)` en passant en singleton-par-tenant, rejetés par PostgREST (`UPDATE requires a WHERE clause`, indépendant de la RLS) — touchait TOUS les tenants dont Abeil, corrigé via `.not("entreprise_id", "is", null)` ; (4) `enregistrerParametragePeriode()` upsertait avec un `onConflict` obsolète (`"annee"` au lieu de `"entreprise_id,annee"`), bloquant tout paramétrage de calendrier — donc la création du premier DJI/congé imposé — pour une année encore vierge, tous tenants confondus ; (5) la fuite RLS `demi_journees_imposees`/`rtt_imposes` documentée ci-dessus. **Même pattern `onConflict` obsolète retrouvé une 3ᵉ fois** (09/09/2026) sur `regles_acquisition` (`enregistrerRegleAcquisition()`, `lib/data/reglesConges.repository.ts`) : cassait 100% des sauvegardes CP et RTT sur Paramétrer > Congés & RTT. Corrigé, vérifié en base. Audit du seul autre `onConflict` du code (`soldes_initiaux.utilisateur_id`) : correct, pas de bug là. Voir MULTI-TENANT.md pour le détail technique de chaque fix. **`apidays.citizen-d.fr` vérifié dans Resend** (09/09/2026, DNS SPF/DKIM sur Gandi corrigés). **E-mails d'invitation testés de bout en bout en prod** (09/09/2026) : premier test réel a révélé que `RESEND_API_KEY` (utilisée pour tout le projet) est restreinte au domaine `abeil-conges.citizen-d.fr` côté Resend, incompatible avec `apidays.citizen-d.fr` — corrigé par une clé dédiée (`RESEND_API_KEY_INVITATIONS`, posée dans Vercel), retesté avec succès (2 tenants de test créés puis supprimés via `/admin`). Voir MULTI-TENANT.md pour le détail. **Reste à faire** : édition/désactivation d'un tenant existant depuis `/admin` (repoussé, pas de besoin identifié) ; configuration DNS/Vercel réelle pour le sous-domaine quand un vrai 2ᵉ client existera (le chemin `/t/<slug>` fonctionne déjà sans ça) ; brander aussi les e-mails de réinitialisation de mot de passe (même mécanisme réutilisable) ; étendre le branding à `--color-slate` une fois "Refacto Design System" fait ; supprimer/neutraliser la couleur orange de test sur `test3` maintenant que le test visuel est terminé. | Faible |
| ~~Révision des tableaux de l'app~~ (→ **fait, 02/09/2026**) (`/historique` à l'origine, 20/08/2026 — élargi et priorité baissée le 28/08/2026, **pattern établi et documenté le 29/08/2026** sur Historique, voir CONTEXTE.md) : teinte "vert header" (titre/sélecteurs/CTA/en-têtes de colonnes + séparateurs), colonnes resserrées à leur contenu (`w-px`+`whitespace-nowrap`) pour Type court/Durée/Statut/Paie, alignement à gauche partout, dates compactes (jj/mm/aa), alpha over/déclenché propres (10%/20%) — **réplication sur Suivre les demandes/soldes 2, Transmissions paie et la liste Utilisateurs confirmée faite par Vincent le 08/09/2026**. Vérifier aussi que les filtres proposés restent pertinents sur TOUS les tableaux (pas seulement Historique) — voir aussi l'item "Uniformisation des éléments UI" plus bas, chevauchement à recouper | Haute |
| ~~Les mini-calendriers du parcours de dépôt de congés n'affichent pas les jours avant/après le mois~~ (→ **caduc, retiré le 28/08/2026**) | Moyenne |
| ~~Administration des FAQ~~ (21/08/2026, fusionné le 28/08/2026 → **mécanisme fait le 07/09/2026**) : nouvelle page Paramétrer > FAQ (`FaqAdminPage.tsx`) — création/modification/suppression, publication, réordonnancement par drag and drop, table `faqs` + RLS. Contenu existant migré tel quel (seed SQL) pour ne pas perdre l'affichage pendant la bascule. **Reste ouvert** : la rédaction des vraies réponses métier validées — le contenu actuel n'est toujours qu'un premier jet, maintenant éditable depuis l'admin mais pas encore réécrit | Moyenne |
| ~~Passer la navigation de second niveau (`SideNav`) en sticky au scroll~~ (→ **fait le 28/08/2026**) : `SideNav.tsx` passé en `fixed inset-0` (remplace `absolute top-0 bottom-0`, qui défilait avec la page faute de conteneur de scroll interne) — le rail remonte jusqu'à `top: 0` pour combler l'espace laissé par `HeaderBar` une fois défilé hors du viewport (`HeaderBar` reste volontairement non sticky, refusé par Vincent — juste `relative z-50` pour rester au-dessus du rail tant qu'il est visible à l'écran) | Moyenne |
| ~~Interactions calendrier collaborateur : définir le fonctionnement (clic sur une date / une card) (21/08/2026)~~ (→ **fait, confirmé par Vincent le 02/09/2026**) | Moyenne |
| ~~Utilisateurs : ajouter un export CSV sur la liste des utilisateurs~~ (21/08/2026 → **fusionné le 28/08/2026** dans "Uniformisation des éléments UI") | Faible |
| ~~Tester le calcul des jours de congés cumulés et les règles de régularisation pour Delphine~~ (21/08/2026 → **fait, confirmé par Vincent le 28/08/2026**) | Haute |
| ~~Suivi solde administrateur ("Suivre les soldes") : afficher le solde théorique ou réel~~ (21/08/2026 → **tranché le 24/08/2026**) : la bascule réel/théorique existait déjà (`SoldeDetailPanel`), reste à trancher le défaut — décidé sur "Théorique" (`modeParDefaut="theorique"`), même défaut que l'écran collaborateur | Faible |
| ~~`/parametrer/calendrier2` : à exactement 768px de large, débordement de la colonne légende~~ (22/08/2026 → **remplacé le 28/08/2026** par un audit global ci-dessous, `CalendrierPage.tsx:623-638` en exemple concret : `md:flex-row` à 768px avec `max-w-[900px]` (grille) + `md:w-64` (légende) qui ne tiennent pas ensemble — même famille de bug qu'un seuil déjà corrigé une fois ailleurs, `md:` remonté à `xl:`) | Faible |
| ~~Décider du sort de `/parametrer/calendrier3`~~ (22/08/2026 → **tranché le 22/08/2026**) : abandonné — route, `Calendrier3Page.tsx` et le lien nav "Calendrier (v2)" supprimés, `/parametrer/calendrier2` reste le seul écran Calendrier (déjà refondu le même jour, voir CONTEXTE.md) | Faible |
| ~~Tiroir "journal" mal calé — plaqué sur le bord de l'écran~~ (22/08/2026 → **fait le 28/08/2026**) : `ActiviteRecenteFeed.tsx` — le panneau était bien positionné dans l'absolu, mais aligné sur le bord droit du vrai viewport plutôt que sur le conteneur centré `max-w-[1180px]` de l'app, visible sur écran large (grand vide gris entre le contenu et le tiroir à 1920px) — corrigé avec le même principe de recentrage CSS que le rail latéral | Moyenne |
| ~~Affichage progressif des composants des écrans~~ (22/08/2026 → **fait le 28/08/2026**) : apparition échelonnée (`animate-stagger-in`, `app/globals.css`) déployée sur toutes les pages principales — Accueil, Historique, Suivre les demandes/soldes 2/calendrier, Transmissions paie (+ ses 2 sous-onglets), Paramétrer > Utilisateurs/Congés & RTT/Calendrier | Moyenne |
| ~~Intégrer la vraie charte graphique Abeil (`Charte-abeil/`) — remplacer la palette de travail et le logo placeholder~~ (→ **fait**) | Haute |
| ~~Dépouiller `documentation-conges/` pour définir les règles de calcul CP/RTT réelles~~ (→ **caduc, remplacé le 28/08/2026** par le point ci-dessous) | Haute |
| ~~Transmissions paie — CSV existant vs bouton "Transmettre" sur l'onglet "Générer l'export" (24/08/2026) : les deux coexistent aujourd'hui (décision provisoire)~~ (→ **tranché le 28/08/2026**) : l'onglet "Générer l'export" a été supprimé — CSV et "Transmettre" vivent désormais ensemble sur "Quels congés transmettre" (`TransmissionsPaiePage.tsx`) | Moyenne |
| ~~Transmissions paie — rejouer une transmission sur une période déjà transmise~~ (24/08/2026, référence à l'onglet "Générer l'export" mise à jour le 28/08/2026 suite à sa suppression — le mécanisme vit désormais sur "Quels congés transmettre" → **vérifié résolu le 15/09/2026**) : bloqué par la contrainte unique SQL (`exports_paie_periode_unique`) + bouton désactivé côté UI une fois un export détecté — un message d'erreur dédié existe bien si l'action est tentée malgré tout en direct (`genererExportPaie` catch le code Postgres 23505 et lève "Un export a déjà été généré pour cette période.", `lib/data/exportsPaie.repository.ts:441-446`, remonté tel quel dans `TransmissionsPaiePage.tsx:427`) — la note d'origine était caduque, ce message existait déjà au moment où elle a été écrite | Faible |
| ~~Transmissions paie — "Poser pour un collaborateur" n'a qu'un seul point d'entrée pour l'instant~~ (24/08/2026, → **obsolète, 28/08/2026**) : le bouton lui-même a été masqué/suspendu (simplification du rôle admin) — la question du second point d'entrée ne se pose plus tant que la fonctionnalité est hors service | Faible |
| ~~Gestion des demandes à cheval : clarté de la transmission (UI) et suivi de ces jours~~ (`DetailCongePanel`, 25/08/2026, renommé et réécrit le 28/08/2026 → **traité le 29/08/2026**) : ajout d'un bandeau "Congé sur N mois" (répartition par mois calendaire, mois réel en cours mis en avant en vert) dans `DetailCongePanel`, visible automatiquement partout où le panneau s'ouvre (Suivre les demandes/soldes/Transmissions paie), sans câblage par appelant — corrige au passage un bug où le feed "Transmis le" ne s'affichait jamais depuis "Vérifier les fiches de paie 2" (`lignesTransmission` jamais passé), wording du feed passé à "Transmis en paie le". Reste en pause (discussion non tranchée avec Vincent, documentée dans le code) : action réelle du bouton "Valider" de "Vérifier les fiches de paie 2" | Haute |
| ~~Finir le chantier "Vérifier les fiches de paie"~~ (25/08/2026 → **traité, 28/08/2026**) : section Soldes (mois précédent/en cours/mouvement) et mode figé de "Générer l'export" construits, 2 bugs réels corrigés — split "CP n / CP n-1+n-2", correctif `fetchHistoriqueCp`/`fetchHistoriqueRtt`, audit exhaustif tous collaborateurs/tous mois et revalidation du format Droit/Pris/Solde laissés de côté (dette mineure, pas bloquants) | Haute |
| ~~Prévoir la gestion des fins de période : report des soldes ou non~~ (27/08/2026, précisé le 29/08/2026 → **caduc, tranché le 15/09/2026** : Vincent confirme que la question ne se pose pas — RTT non consommé sur la période de référence perdu = comportement voulu, connu ; CPA bascule vers le CP de la période suivante = déjà fait (`transfertCpa`, voir "Refonte du moteur de calcul CP/CPA à la bascule de période")) : le volet "rendre tangible" la bascule côté collaborateur a été livré séparément le 14/09/2026 — "Solde N-1" cliquable, panneau `DetailSoldeDepartPanel.tsx` décomposant report/transfert CPA/bonus. Fusionné avec l'item "Suivre les soldes : template de détail du Solde N-1" ci-dessous, qui portait la même demande. | Haute |
| ~~Finir la refonte UI de la fiche utilisateur et revoir le formulaire de création d'un utilisateur~~ (27/08/2026, reprécisé et priorité baissée le 02/09/2026 → **fait, 05/09/2026**) : brouillon `UtilisateurFichePage2.tsx`/`/parametrer/utilisateurs2/` basculé en version définitive (voir CONTEXTE.md). Mode édition : coins carrés, style de champs repris de "Annuler cette demande"/"Ajuster le solde", card identité fusionnée (lecture seule + popin "Modifier" unique), Nature du contrat/Durée de travail avec historique dans deux cards dédiées, card Rôle en colonne de droite, bandeau de statut, "Soldes actuels" sorti du flux (corrections de solde sur Suivre les soldes). Formulaire de création : entièrement repensé et passé en popin (`NouveauUtilisateurModal`) sur la page Utilisateurs, route `/nouveau` supprimée, conventions de formulaire stabilisées (voir Backlog "Améliorer l'UI Congés & RTT" pour le détail). **Checké logique/compréhension du "report de solde"** (05/09/2026) : titre "Solde initial (facultatif)", sélecteurs mois/année (au lieu d'un `input type="month"`), CP/RTT/CPA avec pastilles `TypeBadge` colorées — testé de bout en bout (mois+année+CP/RTT/CPA saisis → vérifié en base, `soldes_initiaux` correctement enregistré). | Moyenne |
| ~~Unifier les boutons "Exporter"~~ (24/08/2026 → **fusionné le 28/08/2026** dans "Uniformisation des éléments UI") : le libellé "Exporter" désigne trois comportements différents selon l'écran — vrai CSV (`Export paie`, `Suivre les soldes`) vs `window.print()` (`Suivre les demandes`, aucun CSV) — sans distinction visuelle sur `Suivre les demandes`. À trancher : soit ajouter un vrai export CSV, soit renommer le bouton ("Imprimer" ?) | Moyenne |
| ~~Trancher l'action concrète de Delphine en cas d'écart~~ (27/08/2026 → **fusionné le 15/09/2026** dans "Validation des fiches de paie — chantier consolidé", volet 2, ci-dessous) | Haute |
| ~~Uniformisation des éléments UI : tableaux, retours, composants~~ (→ **fait, 02/09/2026**) (28/08/2026, remplace l'ancien item "Harmonisation UI restante" du 14/08/2026) : position des filtres (dans la card du tableau vs au-dessus), format des dates (texte "au" vs pill numérique "-"), représentation du statut (`StatusBadge` vs point de couleur fait main), lien "retour" incohérent selon l'écran (`BackHeader` — bouton rond + titre séparé — vs lien fait main `text-ink-500 text-sm` + `ChevronLeft`, ex. `TransmissionsPaiePage.tsx`, un seul vrai appelant de `BackHeader` aujourd'hui : `UtilisateurFichePage.tsx`) ; inclut aussi l'ajout d'un export CSV sur la liste des utilisateurs (`UtilisateursListPage.tsx`, `Paramétrer > Utilisateurs`, seul tableau sans bouton "Exporter" — fusionné le 28/08/2026) | Faible |
| ~~Annulation d'une demande à cheval : lister et prendre en compte les cas à la marge~~ (29/08/2026 → **fusionné le 15/09/2026** dans "Validation des fiches de paie — chantier consolidé", volet 2, ci-dessus) | Faible |
| **Emails d'authentification en HTML stylé** (08/09/2026, scindé de l'item Authentification ci-dessus une fois celui-ci finalisé) : les templates "Invite user"/"Reset Password" sont en français mais toujours en texte brut minimal (`<h2>`/`<p>`/lien) — évaluer une version avec un peu d'UI (logo, couleurs de la charte Abeil) selon l'effort vs le gain pour un simple email transactionnel | Faible |
| **Définir le parcours de création / configuration d'un nouveau tenant / administrateur** (09/09/2026, remarque de Vincent après le bug "test3" — un tenant fraîchement créé via `/admin` n'a ni règles d'acquisition CP/RTT, ni période, ni notifications configurées, sans que rien ne le signale à l'admin avant que ça casse à l'usage, parfois silencieusement, voir CONTEXTE.md) : **"Date de début d'utilisation de l'outil" posée le 11/09/2026** (`entreprises.date_debut_utilisation`, nullable, champ obligatoire à la création `/admin/nouveau`, sans influence sur l'ancienneté réelle du collaborateur) **et volet avertir/bloquer fait le 14/09/2026** (plus tôt dans cette session → **vérifié le 15/09/2026**) : `SoldeIndeterminableError` levée par `resolverCapitalOuvertureCp` quand une période CP est antérieure à cette date sans solde initial saisi, attrapée par `fetchSoldes` (CP seul, la CPA continue de s'afficher normalement) — affiche "Solde non déterminable — saisir un solde initial" directement sur la card solde CP du collaborateur (`lib/data/soldes.repository.ts:1089-1146`). **Reste ouvert** : ce mécanisme couvre uniquement le cas précis "période CP sans solde initial" — pas le signal plus large envisagé à l'origine (bandeau "configuration incomplète" tant qu'aucune règle d'acquisition CP/RTT n'est définie du tout pour le tenant, ou parcours guidé post-création) — cas "test3" d'origine (tenant sans AUCUNE règle paramétrée) toujours pas couvert | Faible |
| ~~Ajouter un pied de page~~ (09/09/2026 → **fait le 10/09/2026**) : `Footer.tsx`, même largeur/centrage que le header (1180px), brandé comme lui (`bg-brand-primary`) plutôt que générique comme le reste du contenu — passe au-dessus du rail `SideNav` (`z-50`, sinon recouvert). Copyright + lien "Mentions légales" (`/mentions-legales`, placeholder) | Moyenne |
| **Checker que les CPI n'interfèrent à aucun moment dans le parcours du tenant Abeil** (10/09/2026, reformulé le 15/09/2026 — remplace l'ex-"Check des règles de gestion des CPI") : Abeil n'utilise pas le système de CPI et Vincent estime que la question de leur bon fonctionnement ne se pose plus tant qu'ils ne sont pas paramétrés — reste seulement à vérifier qu'ils restent invisibles/sans impact sur le parcours réel d'Abeil. Question ouverte en parallèle, pas encore tranchée : faut-il carrément retirer la notion de CPI du paramétrage plutôt que la laisser en dormance | Faible |
| ~~Suivre les soldes : template de détail du "Solde N-1"~~ (10/09/2026, demande de Vincent → **fait le 14/09/2026**, voir l'item "Prévoir la gestion des fins de période" ci-dessus pour le détail) | Moyenne |
| ~~Refonte du moteur de calcul CP/CPA à la bascule de période — arrêter le bricolage~~ (10/09/2026, constat de Vincent après une session de correctifs ponctuels en cascade sur `fetchSoldes`/`fetchHistoriqueCp`/`fetchHistoriqueCpa` → **implémenté et vérifié sur acme ET test3 le 11/09/2026**) : 4 patchs indépendants livrés le même jour (bonus d'ancienneté compté deux fois, solde initial CPA qui s'évaporait à la bascule, mois en cours pas complété, bonus apparaissant dès le 1er jour d'un cycle) — chacun trouvé en testant un cas précis, jamais par construction. Cause racine : **aucune source de vérité unique** pour "quel était le capital de la période N" — cette valeur est recalculée indépendamment à 3 endroits (capital courant, base du report, accrual CPA qui l'anticipe) via des formules dupliquées qui doivent rester synchronisées à la main ; un cas particulier touchant l'une sans toucher les deux autres se désynchronise silencieusement. Chantier proposé : extraire une fonction unique "capital de la période X" utilisée partout (capital courant/report/cible CPA), idéalement avec la valeur d'une période mémorisée une fois fixée plutôt que redevinée à chaque lecture. **Cadre posé le 11/09/2026** (audit + architecture cible, pas encore codé — voir CONTEXTE.md) : (1) un CPA reste décompté du solde CPA jusqu'à la vraie bascule, jamais reclassé en CP avant (annule le comportement du 10/09) ; (2) le bonus d'ancienneté est un jour de CP comme un autre, calculé une seule fois au moment où le capital CP d'une période est déterminé, plus jamais dans la formule d'accrual CPA (supprime le hack "dernier jour de période") — reportable par nature ; (3) architecture retenue : geler le résultat d'une période/d'un mois dès qu'un événement admin le confirme (bascule de période, validation mensuelle du réel via `exports_paie.pris_en_compte`) et ne plus jamais le recalculer — seule la période/le mois en cours reste calculé en direct ; table indicative `soldes_periode`. **Implémenté le 11/09/2026** (même session) : `resolverCapitalOuvertureCp` (source unique), tables `soldes_periode`/`acquisitions_gelees` (gel bascule + gel mensuel à la validation d'un export paie), invariant CP(bascule)=CP(veille)+CPA(veille)+bonus vérifié sur acme (période civile) ET le 14/09/2026 sur test3 (période juin-mai, avec un vrai bonus d'ancienneté à 5 ans en jeu — 50,38 = 22 + 27,38 + 1). Voir aussi l'item "Dépôt d'un CPA à cheval sur deux périodes" ci-dessous, cas limite trouvé en testant le 11/09/2026 et volontairement laissé de côté. | Haute |
| ~~Checker le parcours de pose de congés exceptionnels non comptés (congés sans solde, événement familial, etc.) : bien transmis en paie ?~~ (15/09/2026, remplace l'ex-"Check des couleurs des jours de congés" → **bug confirmé et corrigé le 15/09/2026**, voir CONTEXTE.md) : Vincent a posé un CE (Congé exceptionnel), constaté qu'il n'apparaissait pas sur "Quels congés transmettre" contrairement à un CSS — le filtre de transmission (`fetchDemandesAvecSoldeTransmission`/`fetchDemandesEnAttenteAvant`/`fetchCongesConsommesPeriode`) ne retenait que CP/RTT/CSS depuis la création de l'écran (24/08/2026), sans raison documentée pour exclure CE/RECUP/EVT_FAM (les 4 types "sans solde" ont pourtant été introduits ensemble le 04/08/2026). Corrigé via une constante partagée `TYPES_TRANSMISSIBLES_PAIE` (les 6 types), et le bandeau sticky de "Quels congés transmettre" passe CSS/CE/RECUP/EVT_FAM du rendu texte à la même pastille `TypeBadge` que CP/RTT/CPA. **Régression suite au fix, corrigée dans la foulée** : l'export CSV plantait silencieusement dès qu'un CE/RECUP/EVT_FAM apparaissait (`TypeConsomme` local à `TransmissionsPaiePage.tsx` ne connaissait que 4 des 7 types, `ligne.parType["CE"]` undefined) — étendu en même temps. **Audit des autres filtres de type lancé en parallèle** : aucun autre oubli trouvé, tous les cas passés en revue sont soit exhaustifs sur les 7 types, soit restreints à CP/RTT/CPA avec une raison métier documentée (solde/compteur) — la seule exception trouvée était justement `TypeConsomme` ci-dessus, écarté à tort par l'audit comme "légitime" à cause d'un commentaire du 28/08/2026 devenu caduc après le fix du jour (voir CONTEXTE.md pour la leçon retenue) | Haute |
| **Gestion des droits et suppression d'un profil admin par le manager** (21/09/2026, ouvert en corrigeant le bug "manager ne peut pas créer de profil" — voir CONTEXTE.md) : le correctif du 21/09/2026 se limite à empêcher un manager de créer/modifier/archiver un compte **admin** (RLS `role <> 'admin'` des deux côtés). Reste à cadrer plus largement : un manager doit-il pouvoir supprimer/archiver un profil admin dans certains cas ? Faut-il un mécanisme de délégation ou de double validation pour les actions les plus sensibles sur les comptes admin ? Le garde-fou "toujours un admin actif" (`dernierAdmin`) reste uniquement côté UI, jamais vérifié en base par un trigger/contrainte — à voir si ça doit changer. | Moyenne |
| **Création d'un profil "Admin visiteur"** (21/09/2026, demande de Vincent) : nouveau type de profil sans comptabilisation de jours de congés (pas de solde CP/RTT/CPA à suivre), avec uniquement des droits de modification sur "Suivre" et "Paramétrer" — cas d'usage probable : un consultant/prestataire externe qui doit administrer l'outil sans être un vrai collaborateur de l'entreprise. Reste à préciser : est-ce un nouveau rôle à part entière (à ajouter à l'enum `user_role`) ou une variante du rôle admin actuel avec un flag dédié ; quel périmètre exact de "Suivre"/"Paramétrer" (tout ? une partie ?) ; comment ce profil apparaît (ou pas) dans les listes de collaborateurs, exports, effectifs. | Moyenne |
| **Modifier l'intitulé des compteurs RTT** (02/09/2026, scindé de l'ex-"Paramétrage des congés" — le volet vacances scolaires du même item a rejoint "Prise en compte des dates de vacances scolaires" ci-dessus ; volet "définition de la semaine de CP" du même item d'origine retiré le 15/09/2026, référence questions.md caduque) : renommer l'intitulé des compteurs RTT dans l'UI. Reste à préciser : le nouveau libellé voulu, et où exactement (badges/légendes, formulaire de demande...) | Haute |
| ~~Dépôt d'un CPA à cheval sur deux périodes CP — à étudier~~ (11/09/2026, trouvé par Vincent en testant acme : poser un CPA "31/12/2026 → 08/01/2027" donne un solde "à la date de la demande" de 0j puis -6j après la demande) : le CPA est virtuellement remis à 0 au moment de la bascule (le solde CPA "en anticipation" appartient à la période en cours ; à la bascule il est transféré en capital CP et une nouvelle accumulation CPA démarre à 0 pour la période suivante) — un dépôt à cheval sur deux périodes n'a donc pas de sens tel quel. Premier réflexe (plafonner le picker "Au" à la fin de la période courante, comme pour le CP) rejeté par Vincent ("pas logique, revient en arrière"). **Cadré et implémenté le 16/09/2026** : plutôt que de forcer ce cas via "Congés anticipés" (Parcours A, inchangé), ouverture d'un **Parcours B** — sous le bouton "Congés Payés" existant, une date au-delà de la période en cours est désormais autorisée, avec contrôle de solvabilité contre le capital projeté de la période cible (`fetchCapitalPeriodeFuture`, même formule que `resolverCapitalOuvertureCp` — report + transfert CPA + bonus — mais évaluée à aujourd'hui puisque la période cible n'a pas commencé) ; une demande à cheval est scindée en deux demandes CP distinctes à la soumission. Le CP posé sur la période suivante est affiché exceptionnellement dans le feed "Suivre mon solde" CPA (label "CPN+1", le solde CPA en est décompté pour cohérence avec l'Accueil) avec une mention dans le feed CP normal. Voir le cadrage détaillé dans CONTEXTE.md (session du 16/09/2026). Non commité à la fin de la session — reste à vérifier en conditions réelles la vraie bascule de période. | Haute |
| **Validation des fiches de paie — chantier consolidé** (11/09/2026, regroupé le 15/09/2026 — fusionne "Optimisation UI de Vérifier les fiches de paie 3 + clôturer l'export paie", "Trancher l'action concrète de Delphine en cas d'écart" et "Annulation d'une demande à cheval : cas à la marge" ; **volets 2 et 3 quasi-clos le 21/09/2026**, voir détail) : 3 volets. **(1) UI** — ~~trancher la version définitive de l'écran~~ **tranché le 17/09/2026** : `VerifierFichesPaiePage3.tsx` (le proto d'itération) supprimé, `VerifierFichesPaiePage2.tsx` seul survivant. **Reste ouvert** : messages d'erreur génériques sur les actions sensibles de "Quels congés transmettre" (24/08/2026) — état hétérogène, vérifié encore vrai le 21/09/2026 : "Transmettre" (`TransmissionsPaiePage.tsx:426-429`) remonte déjà le vrai message via `genererExportPaie` (distingue doublon/période trop ancienne/erreur DB), mais "Retirer une demande" (`DetailCongePanel.tsx:377`, fonction `executer`) ignore toujours complètement l'erreur réelle (`catch` sans paramètre) et affiche toujours le même texte fixe, quelle que soit la cause (déjà traité par quelqu'un d'autre, perte de connexion, droit refusé) — probablement le même patron sur Refuser/Signaler comme non pris, à vérifier. **(2) Statuts "passage en paie"/régularisation** — **considéré fait en pratique le 21/09/2026**, sans colonne `statut` dédiée sur `export_paie_lignes` comme envisagé à l'origine (toujours juste `jours_inclus`) : les statuts définitifs existent comme badges calculés côté client, livrés le 18/09/2026 (`BadgeTransmission`, `HistoriqueTable.tsx:91-122`, voir item "Parcours transmission paie" points 8/9/10) — **"À régulariser"**, **"Régul transmise"/"Régul prise en compte"**, **"Transmis N/M"/"Pris en compte N/M"** pour le cas à cheval. Seul point non vérifié : le rattrapage réutilise-t-il bien le "repêchage" existant sur l'ancien "Générer l'export" pour une annulation partielle — jamais testé formellement (`questions.md#suivretransmissions-paie`). ~~**(3) Impact des actions de validation sur les soldes**~~ — **considéré traité par Vincent le 18/09/2026** : bug `sommeTransmis`/`genere_le` déjà corrigé le 14/09/2026 (voir item juste au-dessous) ; les effets de bord annexes (rattrapage tardif hors période, RLS salarié sur ses propres lignes transmises) revus et jugés couverts, sans nouveau correctif de code. | Haute |
| ~~Prévoir le changement de période (bascule) côté administrateur~~ (14/09/2026, demande de Vincent, associé à "Transmissions paie" ci-dessus → **considéré couvert par Vincent le 21/09/2026**) : le collaborateur voit le détail de la bascule au clic sur "Solde N-1" (`DetailSoldeDepartPanel.tsx`, livré le 14/09/2026). **(1) "Suivre les soldes"** — confirmé : `SuivreSoldesPage2.tsx` réutilise le même `SoldeDetailPanel`/`DetailSoldeDepartPanel`, le détail de bascule y est donc déjà accessible. **(2) "Vérifier les fiches de paie"** — `VerifierFichesPaiePage2.tsx` n'utilise pas ce même panneau, mais `PanelJoursMouvement` (décomposition différente : jours du mois consommé/acquisition résiduelle, pas report CP/transfert CPA/bonus ancienneté) — jugé suffisant par Vincent, pas de changement de code. | Haute |
| **Refacto & récap Design System** (fusionné le 02/09/2026 — ex-"Refacto Design System" + "Récap Design System") : mettre à jour les typos, composants, attributs et comportements qui ont divergé du réel au fil des itérations récentes (SoldeCard, boutons, titres de section...), ET documenter les composants récents pas encore répertoriés — notamment le gabarit "Listing" (tiroir "Mes demandes" d'Accueil, `ListingTiroir.tsx`/`DetailCongePanel.tsx` en variante `masquerTypeBadgeBandeau`) créé le 18/08/2026. Objectif commun : repasser sur `/design-system` pour le remettre à jour dans son ensemble. ~~`SoldeCard` et le bouton "Poser un congé" (comportements au survol) documentés le 20/08/2026~~ | Haute |
| ~~`sommeTransmis` borne le solde réel sur `exports_paie.genere_le`, pas sur la date du congé lui-même — un export généré/validé en avance "contamine" un solde à une date antérieure~~ (14/09/2026 → **corrigé le 14/09/2026**, plus tôt dans la même session, commit "Vérifier les fiches de paie : corrige le solde réel post-export et son affichage") : `sommeTransmis`/`fetchLignesTransmises` (`lib/data/soldes.repository.ts:878-885`) filtraient leurs lignes sur `exports_paie.genere_le <= dateReference` seul — un export généré en avance sur des congés futurs contaminait un solde réel arrêté à une date antérieure. Corrigé par l'ajout de `.lte("exports_paie.periode_debut", dateIso(dateReference))`, factorisé une seule fois (utilisé par les 4 écrans `fetchSoldes`/`fetchHistoriqueCp`/`fetchHistoriqueRtt`/`fetchHistoriqueCpa`, pas de duplication à corriger 4 fois) | Haute |
| ~~Vérification de la bascule de période (soldes transférés)~~ (16/09/2026 → **vérifié le 17/09/2026** sur le profil Vincent Mayol/acme) : checker le transfert des CP sur le profil Vincent sur acme, et l'interface de pose de congé hors période (Parcours B, voir item "Dépôt d'un CPA à cheval sur deux périodes CP" ci-dessus et CONTEXTE.md). Exports rejoués de septembre à décembre 2026 : reliquat de 3,5j de CP, solde CPA de 21j, et le CP "Parcours B" à cheval bien pris en compte en deux temps (2026/2027) | Haute |
| **Parcours transmission paie — points d'optimisation** (17/09/2026, ouvert pendant une session de test de la bascule de période sur acme — Vincent alimente cet item au fil de l'eau, à traiter dans une session dédiée ultérieure) : **(1)** ~~"Vérifier fiche de paie" : intégrer les congés exceptionnels (CE/RECUP/EVT_FAM...) sur le récap quand il y en a~~ — **corrigé le 17/09/2026**, voir CONTEXTE.md. **(2)** ~~"Quels congés transmettre" : ajouter de l'espace entre les lignes collaborateurs~~ — **corrigé le 17/09/2026**, voir CONTEXTE.md. **(3)** ~~Côté collaborateur : quel est l'intérêt de voir le statut "Transmis" (le solde réel n'est imputé qu'à "pris en compte", pas à la transmission elle-même) ?~~ — **clos le 17/09/2026** (Vincent, sans changement de code). **(4)** ~~"Vérifier les fiches de paie" : sur la popin-récap solde, ajouter en bas le solde du mois FDP~~ — **corrigé le 17/09/2026**, voir CONTEXTE.md. **(5)** ~~"Suivre les demandes" : caler l'affichage du sélecteur de période, retour à la ligne intempestif~~ — **corrigé le 17/09/2026**, voir CONTEXTE.md (bouton "Exporter" aussi repositionné au passage : sorti de la barre de filtres, au-dessus, fond transparent — même correctif appliqué à "Historique" collaborateur, `HistoriquePage.tsx`). **(6)** Revoir le format du XLS exporté. **(7)** ~~Popin explicative sur le congé "période +1" compté dans le solde CPA~~ — **corrigé le 18/09/2026**, voir CONTEXTE.md (bandeau texte du 16/09/2026 retiré, remplacé par un point d'interrogation cliquable sur la pill "CPN+1" du feed CPA, ouvrant un encart contextuel). **(8)** ~~"Vérifier les fiches de paie" : congé "à régulariser" — dans le suivi de solde, associer un label "Régul" au jour régularisé~~ — **corrigé le 18/09/2026**, voir CONTEXTE.md (badge "Régul transmise"/"Régul prise en compte" dans `HistoriqueTable.tsx`). **(9)** ~~Statut "régularisé" (vert) quand un congé est annulé a posteriori ET que la régularisation correspondante est prise en compte~~ — **corrigé le 18/09/2026**, voir CONTEXTE.md (`StatusBadge` accepte désormais un prop `lignes` optionnel). **(10)** ~~"Quels congés transmettre" : un congé à cheval partiellement transmis (ex. 30/10 → 02/11) s'affiche à tort comme "pris en compte" en totalité~~ — **corrigé le 18/09/2026**, voir CONTEXTE.md (badge "Paie" avec fraction "Transmis N/M"/"Pris en compte N/M" quand toutes les périodes n'ont pas encore de ligne). ~~**(11)** La popin de détail d'un congé (`DetailCongePanel`) n'affiche pas le même historique selon l'écran d'ouverture pour un congé à cheval~~ (ex. CPA 30/10 → 02/11, testé sur l'export de novembre 2026) — **corrigé le 17/09/2026, vérifié dans le code le 21/09/2026** (`VerifierFichesPaiePage2.tsx:627`, `lignesTransmission={lignesTransmissionCompletes}` — fetch dédié `fetchLignesTransmissionParDemande`, toutes périodes, à la place des lignes scopées à l'export), voir CONTEXTE.md pour le détail (cause commune à Page2/Page3 : `fetchCheckFichesPaie` scope un seul export). **(12)** Que faire du bouton "Ajuster le solde" (`VerifierFichesPaiePage2.tsx`/`SoldeDetailPanel.tsx`) — rôle/pertinence à clarifier dans le cadre du parcours transmission paie | Moyenne |
| ~~"Calendrier consolidé" : remplacer les 3 onglets (En cours/Période CP/Année suivante) par la fenêtre glissante 9 mois + "Commence : Aujourd'hui/Il y a 3 mois"~~ (17/09/2026, demande de Vincent — "appliquer le même système d'affichage que les calendriers collaborateurs plutôt que les anciens filtres" → **fait et vérifié en live le 17/09/2026**) : `CalendrierGlobal.tsx` utilisait encore le système à 3 onglets déjà abandonné sur `DashboardPage.tsx` le 14/09/2026 ("trop compliqué à l'usage") — remplacé par la même fenêtre glissante/`SelectCommence`, formule reprise à l'identique. Seule la navigation change, le rendu heatmap reste inchangé | Haute |

## V2 — pistes pour plus tard, volontairement hors scope proche

Idées qui changeraient des règles métier plus profondes que ce qu'on traite dans l'immédiat — notées
pour ne pas les perdre, mais explicitement écartées d'une prochaine session pour éviter de s'embarquer
dans une refonte trop large ("usine à gaz").

| Sujet | Origine |
| --- | --- |
| **Formulaire unique "Poser un congé payé" avec répartition automatique CP/CPA** (16/09/2026, proposition de Vincent, écartée le même jour — "on va mettre ça dans une V2, on s'embarque pas dans un tunnel d'usine à gaz") : plus de choix CP/CPA explicite pour l'utilisateur — un seul formulaire, le moteur pioche automatiquement dans les soldes disponibles. Dates dans la période de référence + solde CP suffisant → CP normal. Dates dans la période de référence + solde CP insuffisant → CP jusqu'à épuisement puis reliquat en CPA, **même sans changement de période** (CPA comme mécanisme de dépassement/anticipation en cours de période, pas seulement pour une date future — rupture par rapport à la règle actuelle). Dates après la période de référence → consommation sur le solde CPA (+ report éventuel de CP, détail non précisé). Impliquerait qu'une seule demande puisse se scinder en deux consommations distinctes (part CP + part CPA). Voir [CONTEXTE.md](CONTEXTE.md) et le cadrage de la refonte CP/CPA (session du 16/09/2026) pour le détail de la discussion — l'architecture retenue pour la prochaine session (moins ambitieuse) évite ce cas : une demande dont la date est dans une période future non commencée est posée directement comme CP, avec un contrôle de solvabilité contre le capital projeté de cette période. | 16/09/2026 |
